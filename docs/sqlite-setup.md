# SQLite Setup Guide

This project uses SQLite as the database, which is a file-based database that doesn't require a separate server.

## Initial Setup

1. Install dependencies:
   ```bash
   npm install

